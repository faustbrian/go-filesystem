schema_version: 1
tool_version: v1.0.6
tool_checksums_sha256: d43e6a07573371e95d5fcf47c94e78f9983e903ef4eaab8e39f9bfc5dc224525
manifest:
  modules: modules.json
  packages: packages.json
evidence:
  root: .verification
mutation:
  root: .verification/mutation
  imports:
    - module: .
      archive: .verification/mutation/bootstrap/root.zip
      ledger: .verification/mutation/migration-ledger.json
api:
  baselines:
    - module: .
      mode: apidiff
      path: api/baseline.txt
operations:
  - module: .
    gate: fuzz
    steps:
      - type: go-test
        packages: [.]
        fuzz: '^FuzzParsePath$'
        budget: 10s
        timeout: 20m
      - type: go-test
        packages: [./local]
        fuzz: '^FuzzSymlinkContainment$'
        budget: 10s
        timeout: 20m
      - type: go-test
        packages: [./memory]
        fuzz: '^FuzzMetadataAndListings$'
        budget: 10s
        timeout: 20m
      - type: go-test
        packages: [./s3]
        fuzz: '^FuzzLogicalKeyTranslation$'
        budget: 10s
        timeout: 20m
      - type: go-test
        packages: [./ftp]
        fuzz: '^FuzzMalformedListingEntry$'
        budget: 10s
        timeout: 20m
      - type: go-test
        packages: [./sftp]
        fuzz: '^FuzzMalformedListingInfo$'
        budget: 10s
        timeout: 20m
      - type: go-test
        packages: [./internal/redact]
        fuzz: '^FuzzErrorRedaction$'
        budget: 10s
        timeout: 20m
  - module: .
    gate: benchmark
    steps:
      - type: go-test
        packages: [./local, ./memory]
        benchmark: '^Benchmark(LargeObjectStreaming|Streaming|Listing)$'
        budget: 1x
        timeout: 20m
